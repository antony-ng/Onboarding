import { Application } from "pixi.js"

export class GameDefine {
    static App: Application
    static REEL_COUNT: number = 5;
    static DISPLAYED_SYMBOLS: number = 3;
    static REEL_WIDTH = 130;
    static SYMBOL_SIZE = 150;
}